
const CategoryButton = ({name}) => {

    return (
        <div>
            <button className="bg-gray-100 rounded-md p-2 m-2 font-semibold">{name}</button>
        </div>
    )
};

export default CategoryButton;